let arr = [];
let product = [
  {
    _id: 1,
    dateTime: { hour: 11, day: 9, month: 11, year: 2022 },
    inactiveTime: 10 + 00,
  },
];

function main(data) {
  products.forEach((pd) => {
    if (arr.length > 0) {
      arr[0].price += 2;
    } else {
      arr.push(product);
    }
  });
}
main();
console.log(arr);
